# Multiple regression template

The model predicting `[outcome]` from `[predictors]` was
`[significant/not significant]`, `F(df1, df2) = [value]`, `p = [p]`,
R² = `[value]`, adjusted R² = `[value]`. `[diagnostic summary]`.
For each focal predictor report B, SE, standardized coefficient when computed,
test statistic, p-value, and 95% CI. State whether robust standard errors were
used and interpret associations without causal claims.

Replace every placeholder only with values produced in this run.
