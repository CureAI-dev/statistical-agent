# One-way group comparison template

`[test]` found `[a/no]` group effect on `[outcome]`,
`[statistic](df1, df2) = [value]`, `p = [p]`, `[effect] = [value]`,
95% CI `[if available]`. `[assumption summary]`. `[post-hoc method]` with
`[multiplicity correction]` showed `[pairwise findings]`. Group descriptives
were `[brief values]`. `[plain interpretation and limitation]`.

Replace every placeholder only with values produced in this run.
